import { StyleProp, ViewStyle } from 'react-native';

type PlayerProgressbarPropsTypes = {
  hideTimes?: boolean;
  overrideThumbWidth?: number;
  overrideContainerStyle?: StyleProp<ViewStyle>;
};

export type { PlayerProgressbarPropsTypes };
